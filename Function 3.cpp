#include<iostream>
#include<conio.h>
using namespace std;
/*int add( int n)
{
    if (n==1)
    {
        return 1;
    }
    return n + add(n-1);
}
int main()
{
    int n;
    cout<<" PLZ Enter the value of n:";
    cin>>n;
    cout<<add(n);

}
*/
/*
int add ( int start , int end)
{
    if ( start > end)
    {
        return 0;
    }
    return start + add ( start + 1,end);
}
int fact (int n)
{
    if ( n==0 || n==1)
    {
        return 1;
    }
    return n *fact(n-1);
}
int main()
{
    int arrfactorial []={1, 3, 5, 7};
    for (int i = 0; i<4; i++)
    {
        int n = arrfactorial[i];
        int factorialresult=fact(n);
        cout<<" the factorial of "<< n << "is" << factorialresult << endl;


    }
    int n;
    cout<<"PLZ Enter the initial value: ";
    cin>>n;
    int A = 25;
    int sumresult = add(n , A);
    cout << " sum from "<< n << "to" << A << "is" <<sumresult<< endl;

    float result = static_cast <float>  (sumresult) / fact (7);
    cout << "Result (Sum / Factorial) is " << result <<"\n";
}*/

/*void add( int x , int y)
{
    int res ;
    res = x + y;
    cout << " the add = "<<res;
}
void sub (int a ,int b)
{
    int res ;
    res = a - b;
    cout << "the sub= " << res;
}
int main ()
{
    int x , y;
    cout << " PLZ Enter the values: ";
    cin >> x >>y;
    add( x , y);
    sub (x , y);
}*/
/*void add ();
void sub ();
int x , y;
int main ()
{
    cout << " PLZ Enter the values: ";
    cin >> x >>y;
    add();
    sub();
    return 0;
}
void add ()
{
    
    int res ;
    res = x + y;
    cout << " the add = "<<res;

}
void sub ()
{
    int res ;
    res = x - y;
    cout << " the sub = "<<res;
}*/

